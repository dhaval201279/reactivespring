gatling {
  charts {
    noReports = false        # Ensure reports are generated
    statsTsvSeparator = "\t" # Use tab-separated values for easier analysis
    maxPlotsPerSeries = 100  # Maximum number of data points on the graphs

    indicators {
      lowerBound = 500  # Lower bound of acceptable response time (ms)
      higherBound = 2000 # Higher bound of acceptable response time (ms)
    }
  }
}